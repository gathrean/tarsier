# v0.4.4 — Misc Fixes & Polish

> **Date:** 2026-03-18
> **Phase:** v0 MVP polish
> **Depends on:** v0.4.3 (home screen redesign, lesson icons, v0.7 schema)

---

## Summary

12 independent quality-of-life fixes and polish items. Each is self-contained.

---

## TODO 1 — Alam Mo Ba: floating lightbulb above Continue button

Remove the current collapsible "Alam Mo Ba?" button below the progress bar. Replace with a floating lightbulb icon (`lightbulb.fill`) positioned above the Continue/Check button, bottom-right corner.

- Icon: ~32pt, gold (#F5A100), circular background (gold 10% opacity), subtle shadow
- Tapping opens a tooltip/popover with the fact text. Tap outside or lightbulb again to dismiss.
- Only visible on cards with `alam_mo_ba_inline` field
- Position: ~12pt above Continue button, right-aligned with button's trailing edge

**Files:** `LessonView.swift` (remove inline collapsible, add floating overlay)

---

## TODO 2 — Remove AI practice mode

Gate AI practice behind `FeatureFlags.aiPracticeEnabled = false`. Remove AI Practice card from `ChapterDetailView`. Keep code files but disable entry points.

**Files:** `ChapterDetailView.swift`, new `FeatureFlags.swift`

---

## TODO 3 — Introduction chapter has no practice mode

Ensure Chapter 0 (Introduction) never shows a practice node. Add `has_practice` flag to `chapters.json` and respect it in `ChapterDetailView`.

**Files:** `chapters.json`, `Chapter.swift`, `ChapterDetailView.swift`

---

## TODO 4 — Chapter detail page: summary above lessons

Add a 2-sentence description between the header banner and lesson list. Data source: `description` field in `chapters.json`.

**Files:** `chapters.json`, `Chapter.swift`, `ChapterDetailView.swift`

---

## TODO 5 — First lesson after onboarding is Introduction

Change `OnboardingLessonView` to load lesson 0 (Introduction / Abakada) instead of lesson 1 (Po & Opo). Ensure Chapter 0 is always unlocked first.

**Files:** `OnboardingLessonView.swift`

---

## TODO 6 — Remove chip design for "po"

Replace `HighlightedPoText`'s pill/chip styling with inline bold + purple text. No background fill or border around "po".

**Files:** `HighlightedPoText.swift`

---

## TODO 7 — Italicise Tagalog words in lesson text

Support `*word*` markdown italics in teach card body text and quiz explanations. Render wrapped words in italic.

**Files:** `HighlightedPoText.swift` or new markdown text component

---

## TODO 8 — Swipe-to-go-back gesture

Ensure left-edge swipe works on ChapterDetailView and LessonView. Fix any custom navigation that suppresses the default iOS back gesture.

**Files:** `ChapterDetailView.swift`, `LessonView.swift`

---

## TODO 9 — Purple gradient header on Words and Profile pages

Apply the same ube purple gradient header from HomeView to Words and Profile tabs. Title text in white bold over gradient.

**Files:** `WordsView.swift`, `ProfileView.swift`

---

## TODO 10 — Replace "tarsier" wordmark with XP progress bar

Remove the "tarsier" text. Add an XP progress bar capsule (~120pt wide, ~8pt tall) in the stats bar area. Track: white 20% opacity, fill: white full. Shows daily XP progress. Also change XP icon/colour to blue (#38BDF8) throughout.

**Files:** `HomeView.swift`, `SessionCompleteView.swift`, `ProfileView.swift`

---

## TODO 11 — Greeting sheet: only on app launch

Make greeting sheet show only on cold launch (app process start). Use a static in-memory flag, not per-view `@State`. Prevent re-showing on tab switches or lesson returns.

**Files:** `HomeView.swift`

---

## TODO 12 — Locked chapters: tappable with greyed-out detail page

Make locked chapter nodes navigate to a greyed-out chapter detail page. Lessons show lock icons. Tapping locked lessons shows a toast message.

**Files:** `ChapterGridView.swift`, `ChapterDetailView.swift`
